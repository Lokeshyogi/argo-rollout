apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rollouts-demo1
spec:
  replicas: 1
  strategy:
    canary:
      canaryService: rollouts-demo-canary
      stableService: rollouts-demo-stable
      trafficRouting:
        alb:
          ingress: rollouts-ingress
          servicePort: 80
      steps:
      - setWeight: 5
      - pause: {}
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: rollouts-demo1
  template:
    metadata:
      labels:
        app: rollouts-demo1
    spec:
      containers:
      - name: rollouts-demo1
        image: argoproj/rollouts-demo:blue
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        resources:
          requests:
            memory: 32Mi
            cpu: 5m
